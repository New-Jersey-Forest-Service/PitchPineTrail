def load_image(file_path):
    # Placeholder function to load an image from the specified file path
    pass

def load_font(font_path, size):
    # Placeholder function to load a font from the specified file path with the given size
    pass

def draw_text(surface, text, font, color, position):
    # Placeholder function to draw text on the given surface at the specified position
    pass

def play_sound(sound_path):
    # Placeholder function to play a sound from the specified file path
    pass

def clamp(value, min_value, max_value):
    # Clamps a value between a minimum and maximum
    return max(min_value, min(value, max_value))